/* @items array will be generated with query in anew selection table */
/* ref. book_label_query.sql */

CREATE TABLE label (
    id INT PRIMARY KEY,
    title VARCHAR(250),
    color VARCHAR(100)
);


